#!/bin/bash
# Simple shell script to list all Gmail labels

# Make the script executable with: chmod +x list_all_labels.sh
# Usage: ./list_all_labels.sh

# Run the Python script
python list_all_labels.py

# Exit with the same status as the Python script
exit $?
